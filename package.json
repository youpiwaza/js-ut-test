{
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-jest": "^4.0.3",
    "gulp-jest-cli": "^0.0.3",
    "jest": "^24.9.0",
    "jest-cli": "^24.9.0"
  },
  "scripts": {
    "test": "jest",
    "testCoverage": "jest --coverage"
  }
}
